package com.example.phoneinfo.ui.theme

import androidx.compose.ui.graphics.Color

// Background Gradient Colors
val BgGradientStart = Color(0xFF0F172A) // Deep Slate
val BgGradientMiddle = Color(0xFF1E1B4B) // Deep Indigo
val BgGradientEnd = Color(0xFF000000) // Pitch Black

val AppBackground = Color(0xFF0A0A1E)

// Glassmorphism Colors
val GlassBackground = Color(0xFFFFFFFF).copy(alpha = 0.05f)
val GlassBackgroundHighlight = Color(0xFFFFFFFF).copy(alpha = 0.12f)
val GlassBorder = Color(0xFFFFFFFF).copy(alpha = 0.2f)
val GlassShadow = Color(0xFF000000).copy(alpha = 0.25f)

// Vibrant Accents
val AccentCyan = Color(0xFF00F0FF) // Cyber Cyan
val AccentPurple = Color(0xFFB026FF) // Neon Purple
val AccentPink = Color(0xFFFF007F) // Neon Pink
val AccentGreen = Color(0xFF00FF66) // Neon Green
val AccentOrange = Color(0xFFFF6A00)
val AccentBlue = Color(0xFF3D8BFF)    // Hardware / Bluetooth
val AccentYellow = Color(0xFFFFD600)  // Camera / GPU
val AccentTeal = Color(0xFF1DE9B6)    // Memory / Apps

// Text Colors
val TextPrimary = Color(0xFFFFFFFF)
val TextSecondary = Color(0xFFA0AAB2)

// Legacy Material 3 defaults (kept for compatibility)
val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)
val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)
